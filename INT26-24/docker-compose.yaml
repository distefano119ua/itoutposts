services:
  monitor-api:
    build: .
    container_name: monitor-api
    restart: unless-stopped
    ports:
      - "7777:7000"

    env_file:
      - .env.example
    
    volumes:
      - ./src/app/logs:/app/logs
      - ./src/app/cars:/app/cars
      #- /:/host:ro,rslave
      #- /proc:/host/proc:ro

  mongodb:
    image: mongo:7
    container_name: monitor-mongodb
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data:

networks:
  itouposts-network:
    driver: bridge